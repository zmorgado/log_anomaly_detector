# Python 3.12 required (TensorFlow does not support 3.13+).
#   python3.12 -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt

# TensorFlow 2.16.1 does `import distutils`, removed from the stdlib in 3.12
# (PEP 632). setuptools <70 ships distutils-precedence.pth, which registers a
# shim at interpreter startup. Newer setuptools drops it and TF fails to
# import. Pinned deliberately - do not bump without re-testing the TF import.
setuptools==69.5.1

numpy==1.26.4
pandas==2.1.4
scikit-learn==1.3.2
xgboost==2.0.3
tensorflow==2.16.1
joblib==1.6.0

matplotlib==3.8.4
seaborn==0.13.2
